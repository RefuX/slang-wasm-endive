# The annotation processor parses the ~150 MB slang-wasm-lib.wasm at compile
# time; give the Gradle daemon plenty of headroom (javac itself is forked with
# its own -Xmx in build.gradle).
org.gradle.jvmargs=-Xmx6g
